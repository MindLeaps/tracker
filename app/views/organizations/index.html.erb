<%- content_for :title, t(:organizations).capitalize %>

<%= render LayoutComponents::HeaderComponent.new(current_user: current_user, tabs: [
  {title:  CommonComponents::TagLabel.new(label: t(:organizations).capitalize, img_src: 'world.svg'), href: '' },
], buttons: [
  (CommonComponents::ButtonComponent.new(label: t(:add_organization), href: new_organization_path) if policy(Organization).new?)
].compact) %>

<%= render TableComponents::Table.new(pagy: @pagy, rows: @organizations, row_component: TableComponents::OrganizationRow) %>
