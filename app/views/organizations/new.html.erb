<%- content_for :title, t(:new_organization) %>

<%= render LayoutComponents::HeaderComponent.new(current_user: current_user, tabs: [
  {title: CommonComponents::TagLabel.new(label: t(:new_organization), img_src: 'world.svg'), href: '' },
], buttons: [
].compact)  do |h|
  h.with_left do
  end
end %>

<%= render OrganizationFormComponent.new(organization: @organization, countries: @countries, action: :create) %>
