<%- content_for :title, t(:subjects).capitalize %>

<%= render LayoutComponents::HeaderComponent.new(current_user: current_user, tabs: [
  {title: CommonComponents::TagLabel.new(label: t(:subjects).capitalize, img_src: 'subject.svg'), href: '' },
], buttons: [
  (CommonComponents::ButtonComponent.new(label: t(:add_subject), href: new_subject_path) if policy(Subject).new?)
].compact) %>

<%= render TableComponents::Table.new(pagy: @pagy, rows: @subjects, row_component: TableComponents::SubjectRow) %>

