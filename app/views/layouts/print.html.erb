<!DOCTYPE html>
<html class="h-full bg-gray-100 overflow-hidden">
<head>
  <title>Tracker<% if content_for? :title %><%= " - #{content_for :title}" %><% end %></title>
  <meta name="turbo-cache-control" content="no-preview">
  <meta content="notranslate" name="google" />
  <%= favicon_link_tag 'favicon.ico' %>
  <%= stylesheet_link_tag 'tailwind.css', media: 'all' %>
  <%= javascript_importmap_tags %>
  <%= yield :head_content %>
  <link href="https://fonts.googleapis.com/css?family=Roboto" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/frappe-gantt/dist/frappe-gantt.css">
  <% if staging_site? %>
    <meta name="robots" content="noindex, nofollow">
    <link rel="canonical" href="https://mindleapstracker.org<%= request.path %>" />
  <% end %>
  <style>
    #group_enrollments .gantt-container {
        height: fit-content;
        overflow: visible;
    }

    #group_enrollments .gantt {
        margin-top: 5px;
    }

    #group_enrollments .current-highlight,
    #group_enrollments .current-date-highlight {
        background-color: rgb(126 34 206/var(--tw-bg-opacity));
    }

    .timeline-pill.today,
    #group_enrollments .side-header,
    #group_enrollments .popup-wrapper {
        display: none;
    }

    #group_enrollments .grid-header {
        position: static;
    }

    .timeline-pill {
        pointer-events: none;
    }

    .timeline-pill .bar-label {
        font-size: .75rem;
    }

    .timeline-pill .bar {
        fill: #4CAF50 !important;
        opacity: .5;
    }

    @media print {
      @page {
        size: auto;
        margin: 0;
      }

      .ct-chart, .ct-chart-bar {
        min-height: 300px;
      }

      .pagebreak {
          clear: both;
          page-break-after: always;
      }

      #group_enrollments .gantt-container {
          width: 100%;
      }
    }
  </style>
</head>
<body class="h-full bg-gray-200">
<div class="h-full flex">
  <div class="flex flex-col h-full w-full overflow-auto print:overflow-visible p-2 print:p-0">
    <%= yield %>
  </div>
</div>
</body>
</html>
