<%- content_for :title, t(:new_country) %>

<%= render LayoutComponents::HeaderComponent.new(current_user: current_user, tabs: [
  { title: CommonComponents::TagLabel.new(label: t(:new_country), img_src: 'flag.svg'), href: '' },
], buttons: [
].compact)  do |h|
  h.with_left do
  end
end %>

<%= render CountryFormComponent.new(country: @country, action: :create) %>
