-content_for :title, "#{t(:edit_student)} - #{@student.proper_name}"

- content_for :header_content do
  = render partial: '/shared/header_content', locals: { back_button_url: student_path(@student), back_tooltip: "Back to #{@student.proper_name}", title: "Edit #{@student.proper_name}" }

.content-card--most.center
  = render partial: 'shared/student_form', locals: {student: @student, action: 'update'}
