<%= turbo_stream.replace('students-table', GroupEnrolledStudentsComponent.new(students: group_students, group: @group, new_student: Student.new(current_group_id: @group.id))) %>
<%= turbo_stream.update('flashes', partial: 'shared/flashes') %>
