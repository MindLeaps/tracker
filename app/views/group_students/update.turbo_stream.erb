<%= turbo_stream.replace(@student, TableComponents::StudentTurboRow.new(item: @student, item_counter: @students.find_index(@student), group: @group, pagy: @pagy)) %>
<%= turbo_stream.update('flashes', partial: 'shared/flashes') %>
