init_config:

instances:
   -   host: localhost
       port: 5432
       username: datadog
       password: {{ datadog_postgres_user_password }}

       tags:
            - replica
            - {{ datadog_env_tag }}-replica
