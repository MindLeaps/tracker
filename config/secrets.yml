# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rake secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

development: &default
  secret_key_base: cb6fa79fcb5eb4e5bc752d1a82778666345996a191f04e045ec7b2c01cf2e4049eef1268e13c52734c1e02081b38a95d6d320f506919a981c6bd5bac88ffab0f
  aws_access_key_id: <%= ENV["AWS_ACCESS_KEY_ID"] %>
  aws_secret_access_key: <%= ENV["AWS_SECRET_ACCESS_KEY"] %>
  aws_bucket_name: <%= ENV["AWS_BUCKET_NAME"] %>
  aws_region: <%= ENV["AWS_REGION"] %>

test:
  secret_key_base: 993195304586a59ebb2bf44a7a6f7dfb016f51cc0e05b169da2cc9c0d1da4d8f910b657840dd72e51e543d3881d36cbc3104729596ef961c2f651d5f937262cc

# Do not keep production secrets in the repository,
# instead read values from the environment.
production:
  <<: *default
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
